# v0.2.0

### Added
- New `Textarea` component with RS-themed styling and focus states
- New `Label` component built on Radix UI primitives
- New `Switch` component with gold/green toggle track
- New `Separator` component for section dividers
- New `Alert` component with `default` and `destructive` variants
- WIP component metadata for upcoming registry entries

### Changed
- Landing page "latest" sort now prefers newest registry entries on date ties

### Fixed
- OG image meta tags for social sharing previews
- Registry error on demo WIP component pages
